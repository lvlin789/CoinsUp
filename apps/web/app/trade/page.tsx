import React from 'react';

export default function TempPage() {
    return (
        <div style={{ padding: 40, textAlign: 'center' }}>
            <h1>临时页面trade</h1>
            <p>这是一个临时页面，用于占位。</p>
        </div>
    );
}